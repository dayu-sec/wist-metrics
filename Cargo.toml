[package]
name = "wist-metrics"
version = "0.1.1"
edition = "2024"
license = "Apache-2.0"
rust-version = "1.85"

[lib]
path = "src/lib.rs"

[dependencies]
jumo-derive = "0.1"
serde = { version = "1.0", features = ["derive"] }
wist-contracts = { version = "0.1.1", path = "../wist-contracts" }
